name: build
run-name: ${{ github.actor }} is testing meanwhile
on: [push]
jobs:
  build-and-run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make -j2
      - run: ./test.out